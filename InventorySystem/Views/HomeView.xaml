<UserControl 
    x:Class="InventorySystem.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:local="clr-namespace:InventorySystem.Views"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
    mc:Ignorable="d"
    Height="610" Width="1000">

    <Border Background="#F9FAFB" CornerRadius="10">
        <Grid>
            <!-- Main Content Container with Margin -->
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- KPIs -->
                    <RowDefinition Height="*"/>    <!-- Main Content -->
                </Grid.RowDefinitions>

            <!-- KPI Row -->
            <UniformGrid Columns="4" Grid.Row="0" Margin="0,0,0,20">
                <!-- Total Revenue -->
                <Border Background="White" CornerRadius="12" Margin="8" Padding="15">
                    <Border.Effect>
                        <DropShadowEffect Color="#E5E7EB" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <fa:IconImage Icon="CircleDollarToSlot" Foreground="#6366F1" Width="20" Height="20"/>
                            <TextBlock Text="Total Revenue" Margin="10,0,0,0" Foreground="#6B7280" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding TotalSalesToday}" FontSize="24" FontWeight="Bold" Foreground="#111827"/>
                        <TextBlock Text="{Binding SalesTrend}" FontSize="11" Foreground="#10B981" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Stock Value -->
                <Border Background="White" CornerRadius="12" Margin="8" Padding="15">
                    <Border.Effect>
                        <DropShadowEffect Color="#E5E7EB" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <fa:IconImage Icon="Vault" Foreground="#F59E0B" Width="20" Height="20"/>
                            <TextBlock Text="Inventory Value" Margin="10,0,0,0" Foreground="#6B7280" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding TotalStockValue}" FontSize="24" FontWeight="Bold" Foreground="#111827"/>
                        <TextBlock Text="Total value of assets" FontSize="11" Foreground="#9CA3AF" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Critical Stock -->
                <Border Background="White" CornerRadius="12" Margin="8" Padding="15">
                    <Border.Effect>
                        <DropShadowEffect Color="#E5E7EB" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <fa:IconImage Icon="TriangleExclamation" Foreground="#EF4444" Width="20" Height="20"/>
                            <TextBlock Text="Critical Stock" Margin="10,0,0,0" Foreground="#6B7280" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CriticalStockCount}" FontSize="24" FontWeight="Bold" Foreground="#EF4444"/>
                        <TextBlock Text="Items need restocking" FontSize="11" Foreground="#EF4444" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Productivity / New Clients -->
                <Border Background="White" CornerRadius="12" Margin="8" Padding="15">
                    <Border.Effect>
                        <DropShadowEffect Color="#E5E7EB" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <fa:IconImage Icon="UserPlus" Foreground="#10B981" Width="20" Height="20"/>
                            <TextBlock Text="New Clients" Margin="10,0,0,0" Foreground="#6B7280" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding NewClientsToday}" FontSize="24" FontWeight="Bold" Foreground="#111827"/>
                        <TextBlock Text="Registered today" FontSize="11" Foreground="#9CA3AF" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Main Content Grid -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.8*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Trends & Performance -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Chart -->
                        <RowDefinition Height="*"/>    <!-- Top Products -->
                    </Grid.RowDefinitions>

                    <!-- Revenue Trend -->
                    <Border Background="White" CornerRadius="15" Margin="8" Padding="20">
                        <Border.Effect>
                            <DropShadowEffect Color="#E5E7EB" BlurRadius="15" ShadowDepth="4" Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel>
                            <Grid Margin="0,0,0,20">
                                <TextBlock Text="Revenue Performance" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937" VerticalAlignment="Center"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                   
                                    <Button Command="{Binding ShowDatabaseStatsCommand}" Style="{StaticResource controlButton}" ToolTip="Show Database Stats" Margin="0,0,10,0">
                                        <fa:IconImage Icon="Database" Foreground="#10B981" Width="14"/>
                                    </Button>
                                    <Button Command="{Binding SeedDataCommand}" Style="{StaticResource controlButton}" ToolTip="Generate Sample Data" Margin="0,0,10,0">
                                        <fa:IconImage Icon="WandMagicSparkles" Foreground="#6366F1" Width="14"/>
                                    </Button>
                                    <Button Command="{Binding RefreshCommand}" Style="{StaticResource controlButton}" ToolTip="Refresh Data">
                                        <fa:IconImage Icon="ArrowsRotate" Foreground="#6B7280" Width="14"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                            <lvc:CartesianChart Height="180" Series="{Binding SaleSeries}" LegendLocation="None">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis IsEnabled="False"/>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="Revenue" LabelFormatter="{Binding Formatter}"/>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </StackPanel>
                    </Border>

                    <!-- Top Selling Products -->
                    <Border Grid.Row="1" Background="White" CornerRadius="15" Margin="8" Padding="20">
                        <Border.Effect>
                            <DropShadowEffect Color="#E5E7EB" BlurRadius="15" ShadowDepth="4" Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Top Selling Products" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,15"/>
                            <ItemsControl ItemsSource="{Binding TopProducts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="0,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}" FontWeight="Medium" Foreground="#374151"/>
                                                    <TextBlock Text="{Binding Category}" FontSize="11" Foreground="#9CA3AF"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{Binding Quantity, StringFormat={}{0} units}" 
                                                           VerticalAlignment="Center" FontWeight="Bold" Foreground="#4F46E5"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Health Column (Right) -->
                <Border Grid.Column="1" Background="White" CornerRadius="15" Margin="8" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect Color="#E5E7EB" BlurRadius="15" ShadowDepth="4" Opacity="0.2"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Critical Stock Alerts" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,15"/>
                        
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding CriticalItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#FEF2F2" CornerRadius="8" Margin="0,0,0,8" Padding="12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#991B1B"/>
                                                    <TextBlock Text="Low availability" FontSize="11" Foreground="#B91C1C"/>
                                                </StackPanel>
                                                <Border Grid.Column="1" Background="#FEE2E2" CornerRadius="10" Padding="8,2">
                                                    <TextBlock Text="{Binding Quantity}" Foreground="#991B1B" FontWeight="Bold" VerticalAlignment="Center"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
        <!-- LOADING OVERLAY -->
        <Grid Style="{StaticResource LoadingOverlay}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Control Style="{StaticResource LoadingSpinner}" HorizontalAlignment="Center"/>
                <TextBlock Text="Updating dashboard..." Margin="0,10,0,0" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
        </Grid>
    </Border>
</UserControl>
