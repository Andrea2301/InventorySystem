<Window x:Class="InventorySystem.Views.SaleDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:local="clr-namespace:InventorySystem.Views"
        mc:Ignorable="d" 
        Title="Sale Details" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent">
    
    <Border Background="White" CornerRadius="15" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="#000" BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title -->
                <RowDefinition Height="Auto"/> <!-- Sale Info -->
                <RowDefinition Height="*"/>    <!-- Items List -->
                <RowDefinition Height="Auto"/> <!-- Summary -->
                <RowDefinition Height="Auto"/> <!-- Footer button -->
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <Grid>
                    <TextBlock Text="Sale Transaction Breakdown" FontSize="20" FontWeight="Bold" Foreground="#1F2937"/>
                    <Button Click="Close_Click" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
                        <fa:IconImage Icon="Times" Width="16" Foreground="#9CA3AF"/>
                    </Button>
                </Grid>
            </StackPanel>

            <!-- Sale Info -->
            <Border Grid.Row="1" Background="#F9FAFB" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="ORDER NUMBER" Foreground="#9CA3AF" FontSize="10" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Id, StringFormat='#{0}'}" FontSize="16" FontWeight="Bold" Foreground="#1F2937"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                        <TextBlock Text="SALE DATE" Foreground="#9CA3AF" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding SaleDate, StringFormat='dd/MM/yyyy HH:mm'}" FontSize="14" Foreground="#374151" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Items List -->
            <DataGrid Grid.Row="2" ItemsSource="{Binding SaleDetails}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      IsReadOnly="True"
                      Background="Transparent"
                      BorderThickness="0"
                      HeadersVisibility="Column">
                
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#6B7280"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="0,5"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" Width="*"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="100"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat=C}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Summary -->
            <StackPanel Grid.Row="3" Margin="0,15,0,0">
                <Separator Background="#F3F4F6" Margin="0,0,0,15"/>
                <Grid>
                    <TextBlock Text="Total Amount" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
                    <TextBlock Text="{Binding TotalAmount, StringFormat=C}" FontSize="18" FontWeight="ExtraBold" Foreground="#4F46E5" HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>

            <!-- Footer -->
            <Button Grid.Row="4" Content="Back to History" Click="Close_Click"
                    Background="#374151" Foreground="White" 
                    Height="40" Margin="0,20,0,0" 
                    BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
        </Grid>
    </Border>
</Window>
